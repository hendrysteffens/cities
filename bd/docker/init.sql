CREATE TABLE city(
   ibge_id int,
   uf char(2),
   name varchar,
   capital bool,
   lon float,
   lat float,
   no_accents varchar,
   alternative_names varchar,
   microregion varchar,
   mesoregion varchar
);


